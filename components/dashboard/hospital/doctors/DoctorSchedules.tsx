"use client";

import React from "react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export const DoctorSchedules: React.FC = () => {
	return (
		<Card>
			<CardHeader>
				<CardTitle>Doctor Schedules</CardTitle>
			</CardHeader>
			<CardContent>{/* Add doctor schedules implementation */}</CardContent>
		</Card>
	);
};
