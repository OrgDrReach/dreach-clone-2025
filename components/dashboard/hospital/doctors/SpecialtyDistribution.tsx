"use client";

import React from "react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export const SpecialtyDistribution: React.FC = () => {
	return (
		<Card>
			<CardHeader>
				<CardTitle>Specialty Distribution</CardTitle>
			</CardHeader>
			<CardContent>
				{/* Add specialty distribution chart implementation */}
			</CardContent>
		</Card>
	);
};
