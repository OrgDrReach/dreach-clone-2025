"use client";

import React from "react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export const OnCallRotation: React.FC = () => {
	return (
		<Card>
			<CardHeader>
				<CardTitle>On-Call Rotation</CardTitle>
			</CardHeader>
			<CardContent>{/* Add on-call rotation implementation */}</CardContent>
		</Card>
	);
};
